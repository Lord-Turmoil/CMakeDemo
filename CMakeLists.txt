cmake_minimum_required(VERSION 3.16)

project(CMakeDemo)

# Enable testing requires to be added in top level CMakeLists.txt
enable_testing()

add_subdirectory(chapter_1)
add_subdirectory(chapter_2)
add_subdirectory(chapter_3)
